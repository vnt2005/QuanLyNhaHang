# WORKLOG 04/09/2026

## Tổng quan

Ngày 04/09/2026 tập trung hoàn thiện các công việc còn lại trong kế hoạch 03/09, ưu tiên tính đúng đắn của báo cáo, trải nghiệm CustomerWeb, an toàn thanh toán và chống lạm dụng đơn hàng. Các thay đổi được phát triển ở PR #138, #139, #140 và #141, sau đó được gom vào một PR tổng hợp để kiểm thử và merge vào `main`.

## 1. Chuẩn hóa xuất báo cáo doanh thu CSV/XLSX

- Kiểm tra lại luồng xuất từ màn **Chi tiết báo cáo doanh thu**.
- Tách rõ hai định dạng:
  - **CSV** giữ đúng bản chất dữ liệu thuần, cấu trúc cột rõ ràng, UTF-8 BOM để Excel/WPS đọc tiếng Việt tốt.
  - **XLSX** là workbook OOXML thực sự, không giả lập Excel bằng CSV.
- Bổ sung bảo vệ CSV formula injection cho dữ liệu text bắt đầu bằng `=`, `+`, `-`, `@`.
- XLSX có tiêu đề, nhóm thông tin báo cáo, tổng hợp tài chính, doanh thu theo món, ghi chú, định dạng ngày/VND, freeze header, filter và độ rộng cột phù hợp.
- Chỉnh lại giao diện workbook sau khi kiểm tra bằng WPS:
  - bỏ nền navy quá đậm;
  - dùng tông sáng nhẹ hơn;
  - đưa khối báo cáo vào giữa trang;
  - căn giữa tiêu đề/nhãn/số liệu hợp lý;
  - làm bảng món thoáng và dễ đọc hơn;
  - đổi toàn bộ font workbook sang **Times New Roman**.
- Bổ sung nút xuất XLSX ở danh sách và modal chi tiết; sau khi xuất cập nhật trạng thái báo cáo sang `Exported`.

## 2. Sửa trải nghiệm phân trang ở CustomerWeb

- Sửa trang **Đơn của tôi**: khi bấm `Trang trước` hoặc `Trang sau`, sau khi dữ liệu trang mới tải thành công viewport tự trở lại đầu khu vực danh sách đơn hàng.
- Tính vị trí sticky header để nội dung đầu danh sách không bị che.
- Không cuộn khi request lỗi hoặc request cũ đã bị thay thế.
- Giữ nguyên filter/search khi chuyển trang.
- Hành vi được thiết kế để dùng ổn trên desktop và màn hình nhỏ.

## 3. Khóa lỗ hổng nhân viên/Admin thanh toán hộ khách hàng

### Rà soát và nguyên tắc

- Rà soát payment verification, server-side authorization, least privilege, separation of duties, maker-checker/dual-control, audit trail và xác minh giao dịch SePay.
- Xác định rủi ro chính: Admin WebApp có thể tự ghi nhận thanh toán cho đơn khách chưa trả tiền, khiến hệ thống chuyển sang `Paid` dù chưa có giao dịch ngân hàng hợp lệ.

### Luồng thanh toán mới

- Chuyển màn **Thanh toán** của Admin WebApp thành **màn chỉ xem lịch sử SePay**.
- Bỏ hoàn toàn:
  - `Thanh toán mới`;
  - modal `Thanh toán tại quầy`;
  - lựa chọn tiền mặt/thẻ/ví điện tử để nhân viên tự ghi nhận;
  - sửa payment;
  - hủy/xóa payment.
- Backend `PaymentsController` chỉ còn API GET cho màn Admin; không còn POST/PUT/DELETE để có thể bypass bằng Postman.
- Một payment chỉ xuất hiện ở màn Admin khi:
  - `Payment.Status == Paid`;
  - `PaymentMethod == BankTransfer`;
  - có `PaymentAttempt` liên kết đúng `PaymentId`;
  - provider là `SePay`;
  - `PaymentAttempt.Status == Paid`.
- Payment Cash/thủ công cũ hoặc BankTransfer không có SePay attempt hợp lệ không xuất hiện trong lịch sử thanh toán SePay.
- Manager/Cashier chỉ giữ quyền xem payment theo cấu hình mặc định; không còn quyền tự tạo/sửa/xóa payment.
- Khách đang trong khoảng chờ 5 phút thanh toán không thể được Admin đánh dấu `Paid` thay; chỉ webhook/giao dịch SePay hợp lệ mới hoàn tất thanh toán.

### Đồng bộ và kiểm thử

- Giữ luồng tạo Invoice, cập nhật Order, Notification/SignalR và Revenue Report từ payment SePay đã xác minh.
- Siết audit log để nội dung tiếng Việt lưu ở dạng đọc được mà vẫn che các trường nhạy cảm như password/token/secret.
- Bổ sung/cập nhật test cho giả BankTransfer, bypass API, sai số tiền, duplicate payment, quyền role, payment đã Paid, lịch sử chỉ xem SePay và tính tổng chỉ từ giao dịch SePay xác minh.

## 4. Siết chống spam hành vi đặt đơn rồi tự hủy liên tục

- Bổ sung `CustomerOrderCancellationAbuseGuard` ở Application layer để rule không phụ thuộc frontend.
- Áp dụng cho khách đã đăng nhập khi tạo đơn mang về và gọi món tại bàn qua QR.
- Chỉ tính lần **khách tự hủy** (`Order.CancelledByCustomer`); Admin/hệ thống hủy không làm khách bị phạt oan.

### Cooldown tăng dần

- 1 lần tự hủy gần đây: khóa tạo đơn mới khoảng **5 phút**.
- 2 lần trong 30 phút: khóa khoảng **30 phút**.
- 3 lần trong 6 giờ: khóa khoảng **6 giờ**.
- 5 lần trong 24 giờ: khóa khoảng **24 giờ**.
- 8 lần trong 7 ngày: khóa khoảng **7 ngày**.

> Lưu ý: cơ chế escalation ở trên là phiên bản trung gian trong ngày và đã được điều chỉnh lại ở mục 6 sau khi kiểm tra thực tế.

### Rate limiting

- User đăng nhập: tối đa **8 request tạo đơn / 5 phút**.
- Anonymous: tối đa **4 request tạo đơn / 5 phút**.
- Hủy đơn khách hàng: tối đa **4 request / 30 phút** theo actor.
- Vẫn giữ idempotency để chặn double-click/retry tạo thao tác trùng.
- Giữ business rule chống burst gọi món cùng bàn; rate-limit không che mất validation nghiệp vụ.

### Test

- Unit test toàn bộ các mức cooldown từ 5 phút đến 7 ngày.
- Test xác nhận Admin/system cancellation không kích hoạt hình phạt.
- Integration test cho kịch bản `đặt -> tự hủy -> đặt lại ngay` phải bị backend chặn.
- Regression test cho rate-limit anonymous và chống burst cùng bàn.

## 5. Tổng hợp PR và kết quả merge

Các PR nguồn:

- #138 — Chuẩn hóa xuất báo cáo doanh thu CSV/XLSX.
- #139 — Sửa vị trí viewport sau khi phân trang CustomerWeb.
- #140 — Chuyển Admin thanh toán sang chế độ chỉ xem SePay và khóa thanh toán hộ.
- #141 — Siết chống spam đặt rồi hủy liên tục.

Kết quả:

- Toàn bộ thay đổi được gom vào branch `chore/consolidate-2026-09-04` và PR tổng hợp **#142**.
- **Frontend CI #753**: thành công; build Admin Web và CustomerWeb đều xanh.
- **Backend CI #1136**: thành công; unit tests và integration tests đều xanh.
- PR #142 được **squash-merge** vào `main`.
- Merge commit: `0580cddeb73c4bf80a8d8ab26ae8ef41fa858b76`.
- Các PR nguồn #138, #139, #140 và #141 đã được đóng sau khi #142 merge để tránh merge trùng thay đổi.

## 6. Điều chỉnh chống spam và kiểm tra thông tin người nhận sau khi test thực tế

Sau khi tiếp tục kiểm tra trực tiếp trên CustomerWeb, phát hiện hai vấn đề:

1. Thông tin **Họ và tên / Số điện thoại** của người nhận vẫn cho phép nhập dữ liệu tùy ý, ví dụ tên vô nghĩa hoặc chuỗi số điện thoại quá dài.
2. Cơ chế khóa tài khoản sau một lần tự hủy là quá gắt đối với người dùng bình thường và tài khoản dùng để kiểm thử.

Các thay đổi được thực hiện trong **PR #143**.

### 6.1. Siết validation thông tin người nhận

- Áp dụng validation ở **cả CustomerWeb và backend**, frontend không phải lớp bảo vệ duy nhất.
- Họ tên người nhận:
  - từ **4 đến 60 ký tự**;
  - tối thiểu **2 từ**;
  - chỉ cho phép chữ Unicode, khoảng trắng, dấu nháy và gạch nối;
  - chuẩn hóa Unicode và khoảng trắng trước khi lưu.
- Số điện thoại:
  - chuẩn hóa `+84`/`84` về định dạng `0xxxxxxxxx`;
  - chỉ nhận số di động Việt Nam **10 chữ số**;
  - prefix hợp lệ: `03`, `05`, `07`, `08`, `09`.
- Ghi chú đơn mang về được giới hạn tối đa **500 ký tự** ở backend.
- Bổ sung unit test cho các trường hợp tên/số điện thoại hợp lệ và không hợp lệ.

### 6.2. Chống spam cho khách chưa đăng nhập theo thiết bị + IP

- Bổ sung `AnonymousOrderAbuseMiddleware` trước rate limiter cho request tạo đơn anonymous.
- Theo dõi đồng thời:
  - `X-Client-Id` ổn định của trình duyệt/thiết bị;
  - `RemoteIpAddress`.
- Cùng một thiết bị tạo đơn quá nhanh:
  - lần thứ **5 trong 5 phút** kích hoạt khóa đặt món khoảng **30 phút**;
  - khi thiết bị bị khóa, IP đang sử dụng cũng bị khóa trong cùng khoảng thời gian.
- Thêm ngưỡng phụ theo IP để giảm khả năng lách bằng cách xóa/đổi `X-Client-Id`.
- Retry cùng `Idempotency-Key` không tăng bộ đếm, tránh phạt retry hợp lệ.
- Áp dụng cho cả đơn mang về anonymous và tạo đơn qua QR anonymous.
- Bổ sung unit/integration test xác nhận request guest thứ 5 bị `429 Too Many Requests` và có `Retry-After` tương ứng khoảng 30 phút.

### 6.3. Điều chỉnh rule tài khoản đăng nhập khi tự hủy

- Bỏ cơ chế escalation cũ `1 lần -> 5 phút`, `2 lần -> 30 phút`, `3 lần -> 6 giờ`, `5 lần -> 24 giờ`, `8 lần -> 7 ngày`.
- Rule mới:
  - **1–4 lần tự hủy trong 30 phút**: chưa khóa tạo đơn;
  - từ **lần thứ 5 trong 30 phút**: khóa tạo đơn khoảng **30 phút**, tính từ lần tự hủy gần nhất.
- Chỉ tính `Order.CancelledByCustomer`; đơn do Admin hoặc hệ thống hủy không tính vào vi phạm.
- Việc tính khóa dựa động trên lịch sử 30 phút nên tài khoản test có nhiều đơn hủy cũ không còn bị giữ mức khóa dài nhiều ngày.

### 6.4. CI và merge

- **Frontend CI #754**: thành công; CustomerWeb build xanh.
- **Backend CI #1137**: thành công; unit tests và integration tests đều xanh.
- PR **#143** đã được squash-merge vào `main`.
- Merge commit: `1f1c91cb9ac6e52879b9656b663b55ed1696dd18`.

### 6.5. Kế hoạch sáng 05/09/2026

- Đã tạo `docs/PLAN_2026-09-05.md`.
- Ưu tiên đầu tiên vào sáng 05/09 là chuyển luồng đặt món sang **bắt buộc đăng nhập mới được gửi đơn**.
- Khi khách đang có giỏ hàng nhưng chưa đăng nhập, hệ thống phải giữ lại giỏ hàng, chuyển qua đăng nhập và quay lại đúng luồng đặt món sau khi đăng nhập thành công.
